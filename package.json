{
  "name": "cypress-learn",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:FreaKzero/cypress-learn.git",
  "license": "MIT",
  "dependencies": {
    "cypress": "^4.7.0"
  },
  "scripts": {
    "report:clean": "rm -rf cypress/reports/mochareports && rm cypress/reports/mocha/*.json",
    "report:gen": "mkdir cypress/reports/mochareports && yarn combine-reports && yarn run generate-report",
    "combine-reports": "mochawesome-merge cypress/reports/mocha/*.json > cypress/reports/mochareports/report.json",
    "generate-report": "marge cypress/reports/mochareports/report.json -f report -o cypress/reports/mochareports",
    "test" : "cypress run --browser chrome || yarn report:gen"
  },
  "devDependencies": {
    "cypress-multi-reporters": "^1.4.0",
    "mocha": "^8.0.1",
    "mochawesome": "^6.1.1",
    "mochawesome-merge": "^4.1.0",
    "mochawesome-report-generator": "^5.1.0"
  }
}